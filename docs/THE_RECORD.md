# THE RECORD — every banked day, and what it returned

Derived from `data/nap.db` by `PYTHONPATH=src python -m racing_edge.school.record_export`. **Never edit this file** — edit nothing and re-run; `nap.db` is the source of truth (law 1) and this is only a readable copy of it, so the numbers can be checked from the repo instead of taken on trust.

Level stakes, one point a bet, settled at SP. A pass is shown as a position, not a missing day. The favourite line is the benchmark the ledger already keeps on the same races — it is the thing to beat, never the picker.

_No record yet: `data/nap.db` is absent or empty here. On the box it is written by the 07:30 bank and the 22:00 settle._

