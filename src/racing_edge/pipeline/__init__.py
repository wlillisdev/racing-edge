"""pipeline — orchestration as ONE in-process program (the audit's fix for the
five fragile subprocess pipelines). Functions calling functions, typed objects
passed in memory, no dated-JSON-file hand-offs.
"""
