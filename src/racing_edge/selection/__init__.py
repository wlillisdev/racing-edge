"""selection — stack the method's signals into one transparent case per horse,
apply the veto + let-off, and pick the race (or pass). Pure: no I/O.

This replaces the old black-box 100-point score. A pick is just the horse with
the strongest stack of named reasons, shown in full. No hidden number, no floor.
"""
