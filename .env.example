# .env.example pushed via GitHub MCP

# AI Form Reader (optional) — if ANTHROPIC_API_KEY is absent the form reader degrades
# gracefully (writes an empty result file) and the pipeline is unaffected.
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-haiku-4-5
